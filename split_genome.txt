#!/bin/bash

capture_regions="${2}"
genome="${1}"

while IFS=$'\t' read -r chr start end region; do
  samtools faidx  "${genome}" "${chr}":$((start+1))-"${end}" \
    > "${genome%.fa}"."${region}".fa
done <"${capture_regions}"
